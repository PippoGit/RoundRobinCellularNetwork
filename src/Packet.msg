packet Packet {
    int senderID;
    int receiverID;
    double serviceDemand;  //dimension of packet, random (Filippo: ho messo double per fare una prova...)
    double creationTime;
}

// Non capisco perchè non compili questo file... 